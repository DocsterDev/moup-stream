# Default config

# On command line: "java -Dspring.profiles.active=aws -jar <jar file name>"

server:
  contextPath: /
  port: 8084
  address: 172.31.10.241
  apiUrl: http://172.31.10.241:8083

logging:
  level:
    org:
      springframework:
        web: INFO
  file: spring-stream.log